version: "3.9"

services:
  sunrise-alarm:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: sunrise-alarm
    environment:
     AUTH_TOKEN: ${AUTH_TOKEN}
    restart: "no"  # since it's supposed to run once and exit
    command: ["python", "app.py"]